#ifndef TOKEN_H_
# define TOKEN_H

enum token
{
    EMPTY,
    STATEMENTS,
    ASSIGNEMENT,

    IF,

    ADD,
    MINUS,
    MUL,
    DIV,
    MOD,

    ID,
    NUMBER,
    STRING
};

#endif /* !TOKEN_H_ */
